# Bolt — performance, build, CI, tooling

Findings about performance, build configuration, CI pipelines,
dependencies, and developer tooling. See `.jules/README.md` for the
entry schema.

<!-- Append entries below. Newest at the bottom. -->
