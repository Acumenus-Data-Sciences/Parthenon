"""v0.1.0 mapping benchmark.

3000 (source_concept_id, target_concept_id) pairs sourced from
``vocab.concept_relationship 'Maps to'`` filtered to standard targets,
split deterministically into:

- ``seen.csv``: 2400 pairs whose source_vocab is in the model's
  training corpus.
- ``blind.csv``: 600 pairs from held-out source vocabularies.

Acceptance gates per Plan 6 Task 13:

- top-1 >= 60% / top-5 >= 85% on ``seen.csv``.
- top-1 >= 50% / top-5 >= 75% on ``blind.csv``.

The CSV files themselves are not committed — they are generated by
``scripts/curate_mapping_benchmark.py`` (Task 12) against a real
``parthenon`` DB and gitignored to avoid bundling vocabulary excerpts.
"""
